<form method="post" action="{{ path('app_admin_page_seo_delete', {'id': page_seo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page_seo.id) }}">
    <button class="btn">Delete</button>
</form>
