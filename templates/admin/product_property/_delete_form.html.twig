<form method="post" action="{{ path('app_admin_product_property_delete', {'id': product_property.id}) }}" onsubmit="return confirm('Tem certeza que quer deletar este item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_property.id) }}">
    <button class="btn btn-danger float-start ms-3 text-white">Deletar</button>
</form>
