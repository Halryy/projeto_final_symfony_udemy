{% extends 'base.html.twig' %}

{% block title %}ProductProperty{% endblock %}

{% block body %}
    <h1>ProductProperty</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_property.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ product_property.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_product_property_index') }}">back to list</a>

    <a href="{{ path('app_admin_product_property_edit', {'id': product_property.id}) }}">edit</a>

    {{ include('admin/product_property/_delete_form.html.twig') }}
{% endblock %}
