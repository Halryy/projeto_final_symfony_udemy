{% extends 'base-public.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
    {{ pageSeo.aboutUsPageTitle }}<br>
    {{ pageSeo.aboutUsPageDescription }}<br>
    {{ generalData.address }}<br>
    {{ generalData.email }}<br>
    {{ generalData.phone }}<br>
    {{ globalTags.pixelMetaAds }}<br>
    {{ globalTags.tagsGoogleAds }}<br>
    {{ globalTags.ga4 }}<br>
    {{ urlToPostForm.url }}<br>

    <h1>Notícias</h1>
    <ul>
        {% for news in allNews %}
            <li><a href="{{ path('app_new_detail', {'slug': news.slug}) }}">{{ news.title }}</a></li>
        {% endfor %}
    </ul>
{% endblock %}
